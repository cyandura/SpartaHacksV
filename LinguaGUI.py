import tkinter as tk

m = tk.Tk() #m is the name of the object


record = tk.Button(m, text='Record', width=35)
stop = tk.Button(m, text='Stop and Convert', width=
record.pack()
stop.pack()

m.mainloop()
